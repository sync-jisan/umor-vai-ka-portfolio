// Footer has been integrated into the Contact section for the single-page layout.
// This file is no longer needed but kept empty to avoid build errors if referenced elsewhere (though app is updated).
import React from 'react';
const Footer: React.FC = () => null;
export default Footer;